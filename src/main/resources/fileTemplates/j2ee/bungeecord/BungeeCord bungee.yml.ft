name: ${NAME}
version: '${VERSION}'
main: ${MAIN}
#if (${DEPEND})
depend: ${DEPEND}
#end
#if (${SOFT_DEPEND})
softdepend: ${SOFT_DEPEND}
#end
#if (${AUTHOR})
author: ${AUTHOR}
#end
#if (${DESCRIPTION})
description: ${DESCRIPTION}
#end
