{
    "pack": {
        "description": "${MOD_ID} resources",
        #if (${PACK_COMMENT} != "")
        "pack_format": ${PACK_FORMAT},
        "_comment": "${PACK_COMMENT}"
        #else
        "pack_format": ${PACK_FORMAT}
        #end
    }
}
