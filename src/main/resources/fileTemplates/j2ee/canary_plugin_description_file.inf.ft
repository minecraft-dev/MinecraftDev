name=${NAME}
main-class=${MAIN_CLASS}
#if (${HAS_AUTHOR_LIST})
author=${AUTHOR_LIST}
#end
version=${VERSION}

#if (${ENABLE_EARLY})
enable-early=${ENABLE_EARLY}
#end
#if (${HAS_DEPEND})
depend=${DEPEND}
#end
