#if (${GROUP_ID})
modGroup = ${GROUP_ID}
modVersion = ${MOD_VERSION}
#end
modBaseName = ${ARTIFACT_ID}
#if (${FORGE_VERSION} || ${MCP_VERSION})

#end
#if (${FORGE_VERSION})
forgeVersion = ${FORGE_VERSION}
#end
#if (${MCP_VERSION})
mcpVersion = ${MCP_VERSION}
#end
