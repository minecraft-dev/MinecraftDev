language: java
script: ./gradlew buildPlugin
before_cache:
  - rm -f  $HOME/.gradle/caches/modules-2/modules-2.lock
  - rm -fr $HOME/.gradle/caches/*/plugin-resolution/
cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/
notifications:
  irc:
    channels:
      - "irc.espert.net#mcdev"
    on_success: change
    on_failure: change
    skip_join: true
